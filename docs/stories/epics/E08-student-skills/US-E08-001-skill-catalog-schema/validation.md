# Validation

## Commands

```bash
dotnet build OboxSteam.API/OboxSteam.API.csproj
```

## Expected

- Solution builds with no errors.
- EF migration `AddSkillCatalogAndStudentSkillEvidence` is generated by CLI
  (not hand-edited).

## Not claimed

- Unit / integration / E2E tests (no test project yet).
- Runtime `database update` unless applied locally by the developer.
